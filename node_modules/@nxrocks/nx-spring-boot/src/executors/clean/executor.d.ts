import { ExecutorContext } from '@nx/devkit';
import { CleanExecutorOptions } from './schema';
export declare function cleanExecutor(options: CleanExecutorOptions, context: ExecutorContext): Promise<{
    success: boolean;
}>;
export default cleanExecutor;
