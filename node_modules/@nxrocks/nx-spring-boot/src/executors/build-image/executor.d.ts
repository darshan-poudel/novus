import { ExecutorContext } from '@nx/devkit';
import { BuildImageExecutorOptions } from './schema';
export declare function buildImageExecutor(options: BuildImageExecutorOptions, context: ExecutorContext): Promise<{
    success: boolean;
}>;
export default buildImageExecutor;
