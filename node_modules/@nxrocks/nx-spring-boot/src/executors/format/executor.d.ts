import { ExecutorContext } from '@nx/devkit';
import { FormatExecutorOptions } from './schema';
export declare function formatExecutor(options: FormatExecutorOptions, context: ExecutorContext): Promise<{
    success: boolean;
}>;
export default formatExecutor;
