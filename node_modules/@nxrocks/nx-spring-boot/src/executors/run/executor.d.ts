import { ExecutorContext } from '@nx/devkit';
import { RunExecutorOptions } from './schema';
export declare function runExecutor(options: RunExecutorOptions, context: ExecutorContext): Promise<{
    success: boolean;
}>;
export default runExecutor;
