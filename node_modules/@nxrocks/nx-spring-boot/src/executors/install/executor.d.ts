import { ExecutorContext } from '@nx/devkit';
import { InstallExecutorOptions } from './schema';
export declare function installExecutor(options: InstallExecutorOptions, context: ExecutorContext): Promise<{
    success: boolean;
}>;
export default installExecutor;
